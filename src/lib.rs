mod engine;
mod game;
mod utils;
pub use engine::{Color, Piece, PieceType};
pub use game::*;

pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
